# Alex.ai — Project Status & Full Context

> **Purpose of this file:** complete project context in one place. If you are Claude (or any developer) starting fresh on this repo — read this file first; it tells you what the product is, what's built, how it works, what's decided, and what's left.

Last updated: **2026-07-21** · Status: **v1.2 — web-verification now runs automatically inside analysis (leads found to actually have a website are auto-dropped from the list); THREE lead sources (Google + OSM + TomTom) + Tavily, live at alex-ai-xorvion.vercel.app**

> **Standing rules from Sumit:** (1) keep THIS file updated after every working session; (2) **never add a Claude co-author line to commits**; (3) **per-repo commit identity**: commits on `main` are authored `Sumitkr28 <Sumitkr28@users.noreply.github.com>` (repo-local git config is set), and the `xorvion-main` branch must contain ONLY `xorvion-ai <xorvion-ai@users.noreply.github.com>` commits — to sync it: `git checkout -B xorvion-main main` → restore its README (`git checkout <old-tip> -- README.md` + commit) → `git filter-branch -f --env-filter '<export xorvion-ai identity>' -- xorvion-main` → force-push to xorvion remote.

---

## 1. What Alex.ai is

A **single-user agentic web app** owned by Sumit Kumar (Xorvion). It finds small businesses **anywhere in the world** that have **no website**, stores them as leads with full contact details, deep-analyzes each with AI, and manages the pipeline so Sumit can call/WhatsApp the owner, build them a website with Claude (outside this app), and sell it.

**Business model:** find business with no website → AI writes the pitch + website plan → Sumit contacts via phone/WhatsApp → builds site with Claude → sells it.

## 2. Hard constraints (decided by Sumit — do not change without asking)

1. **$0/month forever — and CARD-FREE.** Sumit will not put a card on file anywhere. Card-free stack: OpenStreetMap (no key) + **TomTom Search (2,500/day free, no card)** for discovery, Gemini free tier for AI, **Tavily (1,000/mo free, no card)** for web verification. Google Places is the only card-adjacent provider (Sumit verified via UPI — trial mode, unbillable; after the 90-day trial it pauses and the other sources continue). Brave was evaluated and REJECTED (2026: card required, auto-bills past $5 credit). The app detects missing keys per source — chips show "· NO KEY". The Quota Guardian hard-stops every keyed provider at 90% of its free limit (tomtom 2,250/day, tavily 900/mo).
2. **Official APIs only, no scraping.** Phone numbers yes; emails are NOT available (no free source) — outreach is call/WhatsApp/visit.
3. **Global** — any city, any country, any language.
4. **Lead-gen only** — no website-builder inside the app (sites are built separately with Claude via the "COPY PLAN FOR CLAUDE" button).
5. **Delete on contact** — marking a lead ✓ CONTACTED **permanently deletes it, with no archive/history copy** (changed 2026-07-21 at Sumit's request — previously it snapshotted to `contacted_archive`). The `/api/archive` endpoint and the dashboard history-CSV tile were **removed (2026-07-28)** — nothing is stored anywhere on contact. The `contacted_archive` table still exists in the DB (holds a few pre-change rows; safe to drop). A confirmation dialog guards the delete.
6. **AI model:** `gemini-3.1-flash-lite` (Sumit's pick — highest free daily limit). Override with `GEMINI_MODEL` env var.
7. **Design is fixed** — pixel-perfect implementation of the handoff in `Agentic AI web app design/design_handoff_alex_ai/` (dark "operator console", Space Grotesk + JetBrains Mono, green `#4ade80`). Don't redesign; follow `README.md` in that folder for tokens.
8. Exactly **one demo lead** ("Annapurna Bhojanalay", marked DEMO) is auto-seeded on first run so every feature is visible — everything else starts empty. All features are real (live APIs), nothing is simulated.

## 3. Stack

Next.js 16 (App Router, TS, Turbopack) · React 19 · Neon Postgres + Drizzle ORM (`pg` driver) · Vercel AI SDK (`ai` + `@ai-sdk/google`) · zod · single-password auth (HMAC-signed cookie, `src/proxy.ts` gate) · plain global CSS (`src/app/globals.css` holds all design tokens — no Tailwind).

## 4. How the system works

- **Discovery** (`src/lib/sweep.ts`): a sweep = country + city + categories + sources. Builds one query unit per category×source. The client polls `POST /api/sweep/step` — each step processes ONE unit (Google: up to 3 pages of Text Search; OSM: one Overpass query) and returns new leads for the live feed. Resumable, stoppable, quota-guarded. **The client sweep state + polling loop live in a client-side singleton (`src/lib/sweep-store.ts`, consumed via `useSyncExternalStore`), NOT in the page component — so a running sweep survives navigation: leave Discover for Leads and the sweep keeps going; return and the live feed/progress is still shown instead of resetting (2026-07-27).**
- **Lead sources** (`src/lib/leadsource/`): `google.ts` (Places API (New) with Enterprise field mask incl. `websiteUri`, `googleMapsUri`, phones), `osm.ts` (Nominatim geocode → Overpass by category tag selectors; free/unlimited, polite 1 req/s), `tomtom.ts` (poiSearch by category text around the Nominatim bbox center, 1 request/category, up to 100 POIs; `poi.url` absent → lead). All normalize to one shape (`types.ts`). A business whose "website" is Facebook/Instagram/WhatsApp/linktree/directory page = `social_only` (still a lead, often the best kind). Real website = skipped.
- **Dedup** (`src/lib/dedupe.ts` + `insertLead` in sweep.ts): unique per (source, source_id); cross-source = similar normalized name within ~150 m.
- **AI analysis** (`src/lib/analyze.ts`): one `generateObject` call per lead → score 0-100 + reasoning, business profile, site plan (content angle, selling points, suggested pages), outreach (WhatsApp EN + local language, 5-step call script, best call window). Google leads also pull up to 5 reviews first (only while Places usage < 70%). Batch mode = client loop on `POST /api/analyze/step` (one lead per call, 5 s apart for the free-tier rate limit, pause/resume). **Auto web-verification is folded in (2026-07-21):** on a lead's first analysis, if a Tavily key exists and its monthly budget is comfortable, `verifyLead` runs first (best-effort — never blocks analysis); if it finds the business DOES have a real website, the Gemini call is skipped and the lead is returned `dropped` (hidden from the leads list — see §5). No manual "Verify on web" click is needed; that button is kept for on-demand re-checks.
- **Web verification** (`src/lib/verify.ts`): Tavily search "name + city" → confirms no real website exists anywhere (sets `verified_no_website`), harvests FB/IG/WhatsApp links into `socials`. **Matching is domain-derived (2026-07-21):** a result counts as "their site" only when the business name — or ≥2 of its significant tokens — is baked into the hostname; a stray token in the page *title* is rejected (fixes "Arizona Shower Door" false-matching a tourism page titled "Arizona"). Deliberately conservative — when unsure it errs toward "no website" so a real lead is never wrongly dropped. Tavily runs at `search_depth:"advanced"` + `max_results:15` (2 credits/search, still within 1,000/mo) so the has-site verdict is stable and doesn't flip between identical runs.
- **Quota Guardian** (`src/lib/quota.ts`): every Google/Gemini/Brave call goes through `guard()` (throws `QuotaExceededError` → HTTP 429 → UI toast) and `spend()` (logs to `quota_usage`). Limits in `src/lib/config.ts`: google_places 1,000/mo (all SKUs pooled, conservative), gemini 1,000/day, brave 2,000/mo.

## 5. Database (Drizzle schema: `src/lib/db/schema.ts`)

`leads` (unique source+source_id, website_status none|social_only, verified_no_website [null=unchecked · true=confirmed no site; **false no longer occurs** — a lead found to have a real website is deleted], score, status new|analyzed, is_demo) · `analyses` (site_plan + outreach as jsonb) · `searches` (sweep runs with queries/cursor/counters) · `activities` (notes + follow-ups via due_at; **any activity moves its lead out of the leads list** into the dashboard ACTIVITY LOG) · `quota_usage` (provider+period) · `contacted_archive` (snapshot jsonb) · `settings` (key/value: hardStop, defaultCountry, defaultCategories, fallbackLanguage, trialEndsAt, seeded).

Create tables with `npm run db:push` (drizzle-kit, needs DATABASE_URL in `.env`).

## 6. Pages & API map

Pages: `/login` (intro splash 3 s → password), `/` Dashboard (stats, follow-ups due, activity log, recent sweeps, batch analysis runner, quota), `/discover` (sweep form + live feed + radar), `/leads` (split view; tabs ANALYSIS · LOG·n (analysis holds score, profile, site plan, outreach drafts and raw data — one scroll, no tab hopping); actions CALL/WHATSAPP/VERIFY/REFRESH/CONTACTED→ARCHIVE), `/settings`.

API: `auth/login|logout` · `sweep` (start) + `sweep/step` + `sweep/stop` · `leads` (list/filters/countOnly) + `leads/export` (CSV) + `leads/[id]` + `[id]/analyze|verify|refresh|contacted|activities` · `analyze/step` (batch) · `dashboard` · `fx` (INR rates) · `quota/console` (real Places usage) · `activities` (log list: search/kind filter/limit+offset) · `quota` · `settings`. (`leads/export` powers the always-visible "CSV ↓" button in the leads header — downloads all matching leads, opens in Excel.) All JSON errors: `{error}`; quota blocks add `{quotaBlocked:true}` with HTTP 429.

## 7. Environment (`.env`, template in `.env.example`)

`DATABASE_URL` (Neon) · `ALEX_PASSWORD` · `SESSION_SECRET` · `GOOGLE_GENERATIVE_AI_API_KEY` (Gemini) · `TOMTOM_API_KEY` · `TAVILY_API_KEY` · optional `GOOGLE_PLACES_API_KEY` · optional `GEMINI_MODEL`. Key sources are documented inline in `.env.example`.

## 8. Verification — COMPLETE (2026-07-19, live APIs)

- `npm run build` ✓ (Next 16, all 20 routes) · `npm run lint` ✓
- Auth: `/` → 307 `/login`; wrong password → 401; correct → HttpOnly signed cookie; API without cookie → 401 ✓
- Neon: connected, `db:push` created all 7 tables ✓
- Demo seed: first dashboard load created exactly 1 DEMO lead (Annapurna Bhojanalay, score 91, full analysis) ✓
- **Live sweep** (Jaipur, tailor, google+osm): 3 Google requests → 60 scanned → 48 no-website leads; OSM step → 2 more; cross-source dedup ran; total 50 real leads ✓
- **Live Gemini analysis** on a real lead: `gemini-3.1-flash-lite`, score 92, reasoning cites the lead's real 738 reviews/4.9★, WhatsApp drafts EN + हिन्दी, site plan pages, call window ✓
- Archive flow: ✓ CONTACTED on a real lead → row appears in history CSV, lead 404s in app ✓
- Quota Guardian logged real usage exactly: google_places 4/1000 (month), gemini 1/1000 (day) ✓
- **2026-07-20 additions (all live):** TomTom-only sweep (7 Jaipur barbers, TT badges) ✓ · triple-source Noida salon sweep: Google 20 + TomTom 99 = 119 leads in one run ✓ · OSM Overpass 504 handled gracefully mid-sweep, then fallback-mirror retry added and verified (39 OSM Noida leads via mirrors) ✓ · Gemini analysis #2 (score 92) ✓ · TomTom lead refresh via Place-by-ID ✓ · verify without Tavily key → polite `.env.example` pointer ✓ · filtered CSV export ✓ · Quota Guardian shows all 4 providers, matching real usage (places 8, gemini 2, tomtom 3, tavily 0) ✓
- **Tavily verified live (2026-07-20):** real lead verify → harvested 6 FB/IG links; after expanding DIRECTORY_HOSTS (magicpin.in etc.) it correctly discovered the lead's hidden real website → verified_no_website=false. Feature working as designed. Only Vercel deployment remains untested.
- **2026-07-21 — auto-verify folded into analysis + verify-precision fix (LIVE E2E VERIFIED against live Neon + Tavily + Gemini; `npm run build` ✓ `npm run lint` ✓):** analysis (`analyzeLead`, single + batch) now runs Tavily verification first on a lead's first analysis (best-effort, budget-gated on `canSpend("tavily")`); a lead found to actually have a website is reported `dropped`, skips the Gemini spend, and is hidden from the leads list, dashboard counts, and the batch queue (all filter `verified_no_website is distinct from false`; `?hasSite=1` shows the dropped bucket). Verify matching is now **domain-derived** (`looksLikeOwnSite` in `verify.ts`): a hit requires the full business name — or **all** significant tokens — baked into the hostname; title-only and partial (≥2-of-N) token hits are rejected. **Two false-positive classes fixed & confirmed live:** (a) "Arizona Shower Door" previously matched tourism page `visittheusa.com` → now correctly returns its real domain `arizonashowerdoor.com` (genuine has-site, stays dropped); (b) "The Wine & Spirits Boutique at Eastbridge" matched a *different* store `joywineandspirits.com` on shared category words → now returns no site and is kept as a lead. Live run: fresh lead #457 auto-verified (no site) → analyzed score 65; #458 recovered into the list; #78 & #459 confirmed genuine has-site with their real domains. Manual "Verify on web" button kept for on-demand re-checks. **Deployed-app note:** the live Vercel app needs `TAVILY_API_KEY` in its env for auto-verify to run (absent key → it silently no-ops, analysis still works).
- **2026-07-21 (follow-up) — "maps ↗" link fix (`build` ✓ `lint` ✓):** the leads-page map link and the DATA `map` row now use a shared `mapsHref()` (`src/lib/client.ts`). Google leads keep their authoritative place URL; OSM/TomTom leads now open a Google Maps **name + address search** instead of a bare `lat,lng` query — the coordinate query dropped an empty pin with no business card (reported confusion: "the map shows no shop but the lead has full details"). The lead's name/phone/address come from the source DB (OSM/TomTom), which lists the business even when Google doesn't pin it at those coords (new/OSM-only businesses — a no-Google-presence lead is a *good* one). Fix applies to all existing leads at render (no data migration); stored `mapsUri` (used by CSV/archive export) is unchanged. Verified by running the shipped helper against real lead data.
- **2026-07-21 (follow-up 2) — lead-action changes (`build` ✓ `lint` ✓, live-tested against Neon):** (a) **Manual "Verify on web"** no longer hides a lead when it finds a website — `verifyLead(id, {hideWhenFound:false})` records the check but leaves `verified_no_website` untouched, and the UI asks "delete this lead? OK/Cancel" (OK → hard `DELETE /api/leads/[id]`; Cancel → keeps it). The **interactive single-analyze** ("ANALYZE THIS LEAD") path now ALSO confirms before deleting — `analyzeLead(id, {dropOnSite:false})` returns the found site without hiding, and the leads page shows the same OK/Cancel dialog (2026-07-28). Only the unattended **batch** analysis still auto-drops silently (it can't prompt per lead). (b) **✓ CONTACTED → DELETE** now hard-deletes with no archive (see §2.5). (c) **Outreach buttons reordered:** WHATSAPP is now the green primary (first), CALL is the secondary outline button (second). Live test confirmed: manual verify on a has-site lead returned the site but left it visible/unflagged; DELETE + contacted both removed the lead with zero `contacted_archive` rows written.
- **2026-07-21 (follow-up 3) — full E2E live test + verify determinism fix + Google Places DOWN:** ran a complete live E2E (auth, pages, dashboard, all lead filters, Gemini analysis, activities, discovery sweep, refresh, delete/contacted, quota metering, production smoke) — **all green** except two: (1) **Verify was non-deterministic** on Tavily `basic`/10 results (a lead's has-site verdict could flip run-to-run) → fixed by `search_depth:"advanced"` + `max_results:15`; direct test now 5/5 runs stable. (2) **Google Places went down then was RESTORED.** Cloud billing had been suspended for a missing payment method (RBI e-mandate); Sumit re-enabled billing via UPI autopay/e-mandate on 2026-07-21, and after propagation **Places API (New) works again — verified HTTP 200 with the app's exact Enterprise field mask** (returns websiteUri + phones). Key config is correct (API restrictions include Places API (New); Application restrictions = None). **Gemini verified independent** (AI Studio free-tier endpoint) — unaffected throughout. **No-bill safety (autopay is live):** Google's free tier is **per-SKU/month** (Essentials 10k, Pro 5k, **Enterprise 1k**); Alex.ai's Text Search uses the **Enterprise SKU → 1,000 free/mo**. Daily "Requests/day" quota caps are **NOT adjustable** on this trial/UPI account, so the app-side Guardian is the only guardrail — and it suffices: `google_places` cap = **1,000/mo**, Guardian hard-stops at 90% = **900 < 1,000 free**, and all Places SKUs pool into that one counter, so no SKU can ever cross its free tier → **zero billing**. (Over the free tier, autopay *would* auto-charge — hence the cap.)
- **2026-07-27 — WhatsApp reachability signal (`build` ✓ `lint` ✓):** `whatsappStatus()` in `src/lib/client.ts` flags each lead — **"on WhatsApp ✓"** (definitive: a wa.me/whatsapp link is in the lead's socials, e.g. an OSM `contact:whatsapp`/`whatsapp` tag now normalized to a wa.me link in `osm.ts`, or a link harvested during verify), else a **market heuristic**: US/Canada → "unlikely · call/SMS market", India/Brazil/Europe/etc. → "likely · WhatsApp market", Global → "unknown". No live check, no dependency, no scraping (a compliant/free WhatsApp-registration lookup doesn't exist — see the offer notes). Shown as a green "WA" badge in the leads list (definitive only) + a whatsapp row in SIGNALS and DATA. Rationale: Sumit operates from India and US SMBs mostly aren't on WhatsApp, so the signal steers channel choice (WhatsApp-market leads = WhatsApp-first; US = FB/IG DM + VoIP). Follow-up idea (not yet built): surface harvested FB/IG links as clickable "message" actions + make the primary CALL/WHATSAPP button adaptive to the signal.
- **2026-07-28 — country flags as IMAGES + leads CSV button + history removed:** flag emoji don't render on Windows/Chrome (they show the "US"/"IN" letters), so `src/components/Flag.tsx` renders Twemoji flag **SVGs** via free CDN (`NAME_TO_ISO` maps the app's country names → ISO2) — real flags now show in the CountryDropdown and on each lead's name bar (list rows + detail header). Also this session: an always-visible **"CSV ↓"** export button in the leads header (→ `/api/leads/export`, opens in Excel), and the dashboard history-CSV tile + `/api/archive` endpoint were removed (contact = hard delete, nothing stored anywhere).
- **2026-07-28 — leads list hides uncontactable leads:** `filterConditions` now requires a phone by default (`phone is not null and <> ''`) since email isn't collected, so only contactable leads show in the list, count, sidebar, and CSV export. Opt out with `?noContact=1`. **Impact is large — most OSM leads lack a phone:** e.g. of ~3,946 visible leads only ~1,063 have a phone (OSM 2,830→279 with phone, TomTom 650→375, Google 466→409). The no-phone rows still exist in the DB (recoverable via the param), just hidden.
- **2026-07-28 — purged phone-less leads + OSM now requires a phone:** at Sumit's request, permanently DELETED the 2,883 no-phone non-demo leads (57 google, 2,551 osm, 275 tomtom) — DB went 3,951→1,068, demo preserved. And **OSM discovery now skips any element with no phone** (`sweep.ts` OSM branch: `if (!cand.phone) continue;`) so sweeps stop flooding the DB with uncontactable OSM rows. Google/TomTom still store their few no-phone leads but the default query filter keeps them hidden. Email isn't enforced/collected (Google/TomTom return none; only rare OSM `contact:email`; no email outreach exists) — phone is the enforced contact. **Security TODO:** the Places key was shown in chat screenshots + has no Application restriction → rotate it (console "Rotate key") and update `.env` + Vercel, and set the quota cap to bound any abuse. Production confirmed live (shared Neon DB, `TAVILY_API_KEY` set on Vercel).
- **2026-07-29 — dashboard ACTIVITY LOG card:** `/api/dashboard` now also returns `activityLog` (latest 10 `activities` rows joined to their lead, newest first, follow-ups included) and the dashboard renders it as a card between FOLLOW-UPS DUE TODAY and RECENT SWEEPS — kind, lead name, note, time-ago; clicking a row opens `/leads?sel=<leadId>`. Previously activity notes were only visible inside a lead’s LOG tab. Each row shows the lead’s country flag (`<Flag>`, same as the leads list) — the API selects `leads.country` with it. RECENT SWEEPS was moved to the right column, under FREE QUOTA REMAINING (left column = follow-ups + activity log). No pagination (10 rows, hard cap).
- **2026-07-30 — mobile support (≤820px), verified on a 360px and 390px viewport:** the app is now usable on a phone with every feature intact. Layout-critical inline styles were moved into classes in `globals.css` (`.shell .split .pane-side .pane-main .pg .cols .rail .grid2 .grid3 .data-grid .sweeps-grid .tabs .side .nav-item`) purely so one `@media (max-width: 820px)` block can override them — inline styles can't be reached by CSS. On phones: the sidebar becomes a top bar (logo + quota card hidden, nav in a row, LOCK on the right, lead count dropped so it fits 360px); every two-column layout stacks; the leads split view has the list and detail **take turns** (`.split.swap` + `sel` class, with a `← ALL LEADS` button that is `.mobile-only`) and auto-select-first-lead is skipped below 820px so you land on the list; sweep query labels clamp to 2 lines; tab strips scroll horizontally. Desktop is byte-for-byte the same layout (verified at 1440px). Next.js already emits the `width=device-width` viewport meta — no metadata change needed.
- **2026-07-30 — lead detail collapsed to two tabs:** at Sumit's request DATA, SITE_PLAN and OUTREACH were folded into **ANALYSIS**, so a lead is one scroll (score/why + profile + signals rail → content angle + selling points + suggested pages/COPY PLAN → WhatsApp drafts + call script → RAW LEAD DATA) with only **ANALYSIS** and **LOG·n** left in the tab strip. The site-plan and outreach blocks now render only when an analysis exists (`tab === "analysis" && A`) so an unanalyzed lead shows just the ▶ ANALYZE THIS LEAD card + raw data instead of three “run analysis first” placeholders; the duplicate BEST_CALL_WINDOW card was dropped (it already sits in the analysis rail). Verified at 1440px and 390px.
- **2026-08-02 — worked-leads workflow, activity log v2, social buttons, 20 new categories:** seven changes at Sumit's request, all verified live (disposable `zz-test-*` leads seeded then deleted, at 1440px and 390px):
  1. **No more jump to the top.** Removing the open lead (✓ CONTACTED→DELETE, or the delete-on-has-website prompts) now steps to the **next row** via one `dropCurrent()` helper instead of reloading the list — same on phone and PC.
  2. **A logged lead leaves the working list.** `filterConditions` adds `not exists (select 1 from activities where lead_id = leads.id)` (opt out with `?logged=1`), so once a lead has a note/call/follow-up it lives in the dashboard ACTIVITY LOG. The lead stays open after you add the note (so you can add a follow-up too), it just vanishes from the list behind it. Clicking a log row opens that lead's detail even though it isn't in the list (`loadRows` now always honours `preferId`).
  3. **Activity log v2** on the dashboard: new `GET /api/activities` (search over lead name + note, kind filter, `limit`/`offset`, plus the real `total`), and the card got a search box, ALL/NOTE/CALL/WHATSAPP/VISIT chips and a **SHOW MORE ▾ (n more)** button (+25 a click). Follow-up rows show their due time.
  4. **FACEBOOK ↗ / INSTAGRAM ↗ buttons** next to WHATSAPP in the lead header — rendered only for leads that actually carry those links (`socialLinks()` in `client.ts`).
  5. **20 new business types** in Discover/filters (bakery, butcher, clothing, shoe shop, spa, veterinary, car wash, bike/scooter repair, computer repair, printing, stationery, gift shop, catering, guest house/hotel, travel agent, tuition/coaching, dentist, interior/painter, ac/appliance repair, event/decorator) — 44 total, each with OSM selectors.
  6. **No more “any” as a category.** An any-category sweep used to stamp `category = 'any'` on every lead; the normalizers now fall back to the source's own type (OSM `shop/craft/amenity`, TomTom POI category, Google type) via `cleanCategory()`, and the UI resolves old rows the same way through `categoryOf()`.
- **2026-08-02 (follow-up) — country list = PayPal India's receivable markets:** the 12-country list was replaced with the **81 countries** Sumit can actually be paid from (North America 3, Europe 32, APAC/Middle East 20, Latin America 12, Africa 13, plus India) and “🌍 Global”; everything outside that list is gone. One table now feeds all three consumers — `COUNTRY_TABLE` in `config.ts` ({name, iso, lang}) derives `COUNTRIES` (dropdown labels with flag emoji), `COUNTRY_ISO` (used by `Flag.tsx`, which no longer keeps its own copy) and `LANGUAGE_HINTS` (outreach local language for every new market). `UAE` → `United Arab Emirates` with the old name kept as an alias (no stored rows used it — DB countries are US/MX/IN/ES/BR/DE only). The dropdown (Discover, leads filter, settings) gained a **type-to-filter** box since 82 options don't scroll well. Verified: 82 options, 81 flag images, “kor” → South Korea, at 1440px and 390px.
- **2026-08-02 (final country list) — 45 markets, no Global:** `COUNTRY_TABLE` now holds exactly the 45 countries Sumit named (US, Australia, Brazil, Canada, Switzerland, Liechtenstein, Czech Republic, Denmark, Greenland, Faroe Islands, Austria, Belgium, Croatia, Cyprus, Estonia, Finland, France, Germany, Greece, Ireland, Italy, Latvia, Lithuania, Luxembourg, Malta, Netherlands, Portugal, Slovakia, Slovenia, Spain, UK, Hong Kong, Hungary, Israel, Japan, Mexico, Norway, New Zealand, Philippines, Poland, Sweden, Singapore, Thailand, Taiwan, India) in that order, each with ISO2 + language hint. **“Global” is gone** — a sweep always targets one country; `DEFAULT_COUNTRY` = India for Discover/settings, and a settings row still holding the old “🌍 Global” is migrated to it on read (`getSettings` validates against `COUNTRIES`). The leads filter keeps a no-filter choice as its own value, **`ANY_COUNTRY` = “◍ All countries”** (`CountryDropdown anyOption`), and `countryName()` returns "" for anything not in the table so unknown/legacy values simply don't filter. Verified: Discover 45 options + 45 flags and no All-countries entry; leads filter 46 options; `/api/leads?country=🇮🇳 India` → 241 rows, all `country = India`; unfiltered and All-countries both → 1,599.
- **2026-08-10 — currency card, background batch analysis, quota labels:**
  1. **Currency → INR per lead.** `COUNTRY_TABLE` gained `cur`/`sym` (ISO 4217 + symbol) for all 45 markets and `currencyOf()` resolves a lead's country. New `src/lib/fx.ts` pulls **open.er-api.com** (free, keyless, daily rates), inverts it to INR-per-unit, caches 12h in memory and is metered through the Guardian as a new **`fx` provider (1,000/month cap — real usage ≈ 60)**. `GET /api/fx` serves it; the lead detail's ANALYSIS rail shows a **CURRENCY · <code>** card: “1 USD = ₹95.26” plus an input to convert any amount to rupees (also shown for unanalyzed leads). Verified live: USD 95.26, EUR 109.93, JPY 0.60, TWD 2.96 · $1250 → ₹1,19,070.
  2. **Batch analysis now runs in the background.** The loop moved out of the dashboard component into `src/lib/analyze-store.ts`, the same client-singleton pattern as `sweep-store.ts` — leaving the dashboard for Leads no longer kills it. Verified: started, navigated to Leads for 22s (2 more leads analyzed while away), came back to a live “Analyzing lead 2 of 1531”, then paused. A full browser reload still stops it (same as sweeps) — press RESUME to continue.
  3. **Quota rows state their window.** The sidebar Guardian card now prints `PLACES 77/1000·month` / `GEMINI 0/1000·day` (previously only day rows were labelled), the dashboard card says “923 left this month” / “1,000 left today”, and both list the new FX counter. Settings caps list gained the FX row.
- **2026-08-10 (follow-up) — Places usage read from the Google Cloud console:** optional, read-only Cloud Monitoring sync in `src/lib/gcp.ts` — service-account JWT (signed with `node:crypto`, no new dependency) → OAuth token (`monitoring.read` scope) → `timeSeries.list` for `serviceruntime.googleapis.com/api/request_count` filtered to `places.googleapis.com`, summed month-to-date, cached 5 min, metered as a new **`gcp_monitoring`** provider (5,000/month cap; Google's first 1M Monitoring READ calls/month are free). `GET /api/quota/console` → `{configured, places, since, fetchedAt}`; the dashboard quota card shows **google console · N requests this month · live** under the local Places row, or “not connected — see .env.example”, and Settings lists a CONSOLE SYNC row. **The Guardian still hard-stops on Alex.ai's own counter** — a console figure that can't be fetched must never let a call through. Setup (Monitoring API + service account with `roles/monitoring.viewer` + `GCP_PROJECT_ID` / `GCP_SERVICE_ACCOUNT_JSON`) is documented inline in `.env.example`. Verified: the unconfigured path (endpoint returns `configured:false`, UI shows the hint, nothing breaks); the authenticated read is untested. **Parked 2026-08-10:** service-account key creation is blocked in Sumit’s own org `sumitchoudhary2812-org` by the legacy `iam.disableServiceAccountKeyCreation` constraint (Secure-by-Default; the managed twin already reads Not enforced), and turning the legacy one off didn’t take effect — so no key exists and the dashboard simply shows “google console · not connected”. The feature is dormant, costs nothing and blocks nothing; the local counter is still what the Guardian hard-stops on. To revive it: either get that constraint off (project-scoped override) and add `GCP_PROJECT_ID` / `GCP_SERVICE_ACCOUNT_JSON`, or swap `gcp.ts` to keyless Workload Identity Federation (Vercel OIDC → STS) which sidesteps the policy entirely.
- **2026-08-10 (fx v2) — live market rates, not once-a-day:** `fx.ts` now reads **Coinbase's public exchange-rates feed** first (free, keyless, moves through the day) and falls back to open.er-api.com only for currencies Coinbase doesn't quote — it quotes all 23 the 45 markets need, so the fallback normally never fires (`source: "market"`). Server cache 15 min, the leads page re-polls on the same cadence, and the card footer reads “live market rate · <age>”. Rates print with decimals (1 HUF = ₹0.30) while converted totals round (₹4,763). `fx` cap raised 1,000 → 20,000/month (both endpoints are free; the cap is just a leash). Verified live: EUR 109.93 on the daily feed vs **110.17** on the market feed minutes later; $49.99 → ₹4,763.
- **2026-08-10 (trial countdown) — GCP free-trial end date in the app:** new `trialEndsAt` setting (“YYYY-MM-DD”, default **2026-10-18** = Sumit's current trial, ₹28,321 credit) editable from Settings with a native `<input type="date">` under the Quota Guardian card; clear it after upgrading. `/api/dashboard` returns it and the **MONTHLY COST** tile prints “gcp trial ends 18 Oct 2026 · 69d left” (whole calendar days from local midnight — counting the partial day made it read 70; “ends today” at 0) beneath “guardian armed · caps active” — amber in the final week, and “gcp trial ended …” once past. A one-time cloud routine also pings Sumit on 2026-10-16 (`trig_01GfqLQGaB2r8LSeqG1zY1hc`). Verified: date shows 70d left, editing to 30 Nov persisted and the tile followed, restored to 18 Oct.
- **2026-08-10 (copy-all + currency placement) —** the CURRENCY block is now **its own section**: a compact one-line strip (`currencyBar()`) between the action buttons and the tab strip, so the rate + converter show on **both** tabs and belong to no tab (it was briefly in the ANALYSIS rail, then inside the LOG tab; Sumit wanted it standalone). 48px tall at 1440px, wraps to two lines at 390px. And a **⟩ COPY ALL** button sits next to the lead name: `fullCopyText()` renders the whole page as plain text, top to bottom — name/country, LEAD_id · status · source · refreshed, category/address/rating, score, CONTACT (phone, wa.me link, facebook/instagram, maps), LEAD_SCORE reasoning + model, BUSINESS_PROFILE, SIGNALS, best call window, CURRENCY rate, CONTENT ANGLE, SELLING POINTS, SUGGESTED PAGES, both WhatsApp drafts, the numbered call script, every RAW LEAD DATA pair and the ACTIVITY LOG. Verified: one click → 4,991 chars / 68 lines with every section present; button flips to COPIED ✓; beside-the-log at 1440px, stacked at 390px.
- **2026-08-10 (batch resilience + has-website = delete):**
  1. **The batch no longer pauses on a hiccup.** It was stopping after ~8 leads because one failed `/api/analyze/step` (almost always Gemini's per-minute rate limit) broke the loop. `analyze-store.ts` now backs off and retries — 15s, 30s, 45s, 60s… — showing “rate-limited or a hiccup — retrying in Ns (attempt n/6)”, and only stops on a **Quota Guardian block** or **6 consecutive failures**. Verified by injecting 500s into the step endpoint: it kept running through 3 failures, then analyzed the next lead normally once they stopped.
  2. **A business with a real website is deleted, everywhere, with no confirmation** (Sumit's call). `verifyLead` deletes the row (`deleteWhenFound`, default true), `analyzeLead` deletes before spending Gemini, the refresh endpoint deletes when a site appears (returns `deleted:true`), and the two `window.confirm` prompts are gone — the UI just steps to the next lead. The old hidden “has site” bucket and its `?hasSite=1` filter were removed as dead code, and the **79 rows already flagged `verified_no_website = false` were purged** (1,735 → 1,656 leads).
- Note: ~215 REAL leads (Jaipur tailors/barbers + Noida salons) live in the DB for Sumit to use or archive; SETUP.md was deleted at Sumit's request — key docs now live inline in `.env.example`.

## 9. Repos & deployment

- GitHub targets: `Sumitkr28/Alex-ai` (personal-branded README, local branch `main`) and `xorvion-ai/Alex-ai` (Xorvion-branded README, local branch `xorvion-main` — push it as that repo's `main`). Remotes `sumit` and `xorvion` are configured; repos must exist on GitHub first (the provided PATs cannot create repos).
- Deploy target: **Vercel Hobby via the sumitkr28 account** — import repo at vercel.com/new, add all `.env` vars, deploy. Not yet deployed.
- Local `.env` exists with Sumit's chosen login password and a random SESSION_SECRET (never committed — `.env` is git-ignored); all API keys still empty.

## 10. Open TODOs (state on 2026-07-19 evening)

1. ✅ Neon: connected (screenshot had hidden a '5' in the password), tables created, everything verified.
2. ✅ Gemini key: validated live; model `gemini-3.1-flash-lite` confirmed — no override needed.
3. ✅ Google Places API (New): billing verified via **UPI (no card!)** — trial mode (auto-charge impossible). Key validated with real searches. **Quota caps are NOT adjustable on trial accounts** ("Adjust: No" in console) — and not needed: trial mode can't bill, and the in-app Guardian (verified accurate against Google's own meter) enforces 900/month. If Sumit EVER activates the account, set 35/day caps on SearchTextRequest + GetPlaceRequest FIRST. Still to do (optional): restrict key to Places API (New) in Keys & Credentials; optional usage alert at 30/day. **Never click "Activate full account"** — when the 90-day trial ends, the app auto-falls back to card-free OSM mode.
4. ✅ NEW (2026-07-20): **TomTom** added as third lead source (key set, live-tested: 100 tailor POIs in Jaipur) and **Tavily** replaces Brave for verification (Tavily account created, Researcher plan 1,000/mo, pay-as-you-go OFF — key pending from Sumit, arrives truncated in screenshots so must be pasted as text). Brave permanently rejected (card required, auto-bills past $5 credit).
5. ✅ **DEPLOYED (2026-07-20):** live at **[alex-ai-xorvion.vercel.app](https://alex-ai-xorvion.vercel.app)** via the xorvion-ai Vercel account (imported xorvion-ai/Alex-ai, env vars added, build Ready). The repo's main gets force-pushed on updates — Vercel auto-redeploys on every push.
6. Sumit: **rotate the two GitHub PATs** shared in chat (used for pushes; treat as exposed). The Google/Gemini keys also appeared in chat screenshots — key restriction (3.) mitigates; can regenerate anytime.
7. Future ideas (out of scope for v1): demo-site generator per lead, paid email enrichment, multi-user.

## 11. Key documents

- `ALEX-AI-PLAN.md` — approved product plan (source of truth for behavior)
- `Agentic AI web app design/design_handoff_alex_ai/README.md` — design tokens & screen specs (source of truth for looks)
- `.env.example` — free-tier setup guide · `README.md` — public repo readme
